.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.netlify
.claude
bun.lockb
data.db
*.db
.nvm

# Vendored Strapi (cms/) — generated paths
cms/node_modules
cms/.env
cms/.tmp
cms/build
cms/.cache
cms/dist
cms/.strapi
cms/public/uploads
cms/exports
cms/.snapshot

# Local SQLite database — keep the dir (via .gitkeep), ignore the db files
cms/database/*
!cms/database/.gitkeep

# Local dumps pulled from the remote CMS (contains content + admin/token hashes)
cms/dumps/
